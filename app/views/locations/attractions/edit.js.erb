var $newForm = $("<%= j(render 'locations/attractions/form', attraction: @attraction) %>")
$("#attraction-form").replaceWith($newForm);
$("#new-attraction-btn button").trigger("click");
// $("#attraction-form input[type=text]:first").focus();
