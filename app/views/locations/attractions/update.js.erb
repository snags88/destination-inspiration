var newEl = "<%= j(render 'locations/attraction', attraction: @attraction) %>";

var $updatedElement = $("div[data-attraction-id=" + <%= @attraction.id %> + "]");
$("#close-modal").trigger("click");
$updatedElement.replaceWith(newEl);

