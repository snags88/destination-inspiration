var newEl = "<%= j(render 'locations/attractions/show', attraction: @attraction) %>";
var $newForm = $("<%= j(render 'locations/attractions/form', attraction: Attraction.new) %>")

var $updatedElement = $("div[data-attraction-id=" + <%= @attraction.id %> + "]");
$("#attraction-form input[type=text]").val("");
$("#attraction-form textarea").val("");
$("button.close").trigger("click");
$("#attraction-form").replaceWith($newForm);
$updatedElement.replaceWith(newEl);

