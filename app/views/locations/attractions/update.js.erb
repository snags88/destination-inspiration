$("#modal-background").fadeOut("slow");

var newLi = "<%= j(render 'locations/attraction', attraction: @attraction) %>";
var $updatedElement = $("li[data-attraction-id=" + <%= @attraction.id %> + "]");
$updatedElement.replaceWith(newLi);

